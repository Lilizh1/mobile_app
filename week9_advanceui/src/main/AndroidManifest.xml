<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.he.week9_advanceui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SpinnerActivity" />
        <activity
            android:name=".ListView1_Activity"
            android:exported="false" />
        <activity android:name=".ListView2_Activity" />
        <activity android:name=".ListView3_Activity" />
        <activity android:name=".ListView4_Activity" />
        <activity android:name=".FileBrowseActivity" />
        <activity android:name=".StudentAddActivity" />
        <activity android:name=".StudentQueryActivity"></activity>
    </application>

</manifest>